services:
  database:
    image: postgres
    ports:
      - "5432:5432"
    restart: always
    environment:
      POSTGRES_USER: docker
      POSTGRES_PASSWORD: docker
      POSTGRES_DB: exampledb

  app:
    build: .
    restart: always
    depends_on:
      - postgres
    ports:
      - "5000:5000"